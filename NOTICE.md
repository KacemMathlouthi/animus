# Third-party notices

animus itself is MIT licensed. See [LICENSE](LICENSE).

Assets distributed with this repository remain under their own licenses.

## Fonts

**Geist** and **Geist Pixel Grid**, SIL Open Font License 1.1.

- `apps/api/assets/fonts/` (see `OFL.txt` in that directory)
- `apps/web/public/fonts/` (see `OFL.txt` in that directory)

## Audio

**Bundled background music**, public domain. Per track provenance is recorded in
`apps/api/assets/MUSIC_LICENSES.md`.

## Images

The paintings used for share cards, the landing page and the repository social
preview are generated artwork produced for this project.

## Dependencies

Runtime and development dependencies keep their own licenses. Resolve the full
set from `bun.lock` and the `package.json` of each workspace.
